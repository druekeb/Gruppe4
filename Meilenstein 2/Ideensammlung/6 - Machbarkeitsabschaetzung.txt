Automatische Aktualisierung bei Änderungen der FernUni Struktur
===============================================================

Ansatz 1: Das Answendungsprogramm speichert in einer Tabelle welche Daten in welchen Artikeln verwendet werden. Wenn sich die Daten ändern werden diese Artikel neu gerendert und versioniert.
Vorteile: minimaler Aufwand bei Updates, schnelles Update
Nachteile: komplizierte Verwaltung der Tabelle mit der Daten-Artikel Beziehung, neue Markupbefehle oder Daten schwerer integrierbar.

Ansatz 2: Ein automatischer oder manuell ausgelöster Update Prozess rendert _alle_ Artikel neu und vergleicht das Ergebnis mit der aktuell gerenderten Version. Wenn es einen Unterschied gibt, wird neu versioniert.
Vorteile: unkompliziert, keine zusätzliche Verwaltung nötig, neue Markupbefehle oder Daten können einfach integriert werden
Nachteile: unnötiges renderen von Artikeln, kaum in Echtzeit möglich bei vielen Artikeln.

Ansatz 1 ist möglich aber nicht praktikabel ohne die Markups stark einzuschränken. Sollen nur einfach Marrkup Anweisungen möglich sein, so ist Ansatz 1 Ansatz 2 jedoch weit überlegen.