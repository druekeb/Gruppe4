Allgemeines
Das Anwendungsprogramm ist ein statisches Content Management System
(CMS), vorzugsweise als Webanwendung in PHP programmiert, das auf eine OracleDB
aufsetzt. Die Seitenbestandteile (Formatvorlagen, Texte, DatenbankObjekte)
werden in der Datenbank abgelegt. Zur Veröffentlichung einer Seite wird diese
als statische Seite aus ihren Bestandteilen durch das Anwendungsprogramm
gerendert und mit einer Versionsnummer versehen in der Datenbank abgelegt.
Dieses Vorgehen garantiert zum einen hohe Auslieferungsgeschwindigkeit der
angeforderten Seiten und zum anderen erleichtert es die Versionierung, indem bei
jeder Änderung einer Seite eine Versionsnummer vergeben werden kann. So kann
jederzeit auf eine vorherige Version der Seiten zugegriffen werden.

Automatische Aktualisierung der Website bei DB-Updates
Bei den Bestandteilen der Seiten, die in der Datenbank gespeichert sind,
handelt es einerseits um explizit zu der Seite gehörige Elemente (z.B. Artikel)
und andererseits um Attribute von Datenbankobjekten (z.B. die
email-Adresse eines Mitarbeiters), die zur Vermeidung von Redundanz nur einmal
in einer entsprechenden Tabelle (z.B. Mitarbeiter) gespeichert werden. Neben
der Redundanzvermeidung bieten eingebundene Datenbankobjekte den Vorteil, dass
dem Redakteur Auswahlelemente zur Verfügung gestellt werden können bei der
Editierung der Seite. Außerdem kann bei der Änderung eines Datenbankobjektes
eine automatische Kontrolle stattfinden, ob dieses Objekt in Seiten
der Webpräsenz eingebunden ist. Ist das der Fall, sollen alle Seiten, die dieses
Objekt einbinden, neu gerendert und mit einer neuen Versionsnummer versehen,
veröffentlicht werden.

Rechteverwaltung
Die Anwendung verlangt beim Start eine Anmeldung (Login mit Username,
Passwort). Die Rechteverwaltung kennt zwei Rollen (Administrator und
Redakteur). Wünschenswert ist eine Verfeinerung, die es erlaubt, dem
Redakteur unterschiedliche Bereiche des Menübaums zuzuweisen. In diesen
Bereichen (z.B. einem Fachbereich) steht dann die redaktionelle
Funktionalität (Erstellen und Bearbeiten von Seiten) zur Verfügung.

Workflow
Der Publishing-Prozess ist als Workflow realisiert. Eine erweiterte Anforderung
wäre eine zwingende Beteiligung mehrerer Personen an der Veröffentlichung
einer Seite.

Vorschaufunktion
Eine Vorschau-Funktion erlaubt die Kontrolle der editierten Seite im Kontext der
Site vor der Veröffentlichung. Dazu läuft ein einfacher Webserver im
Anwendungsporgramm.

Templates
Durch die Anwendung werden verschiedene Templates für Seiten zur Verfügung
gestellt, so dass die Einhaltung eines durchgehenden Designs gewährleistet
ist. Die Templates werden ebenfalls in der Datenbank gespeichert.

Gültigkeitsdauer für Seiten
Ein Content-Life-Cycle-Management sorgt dafür, dass Inhalte, die mit zeitlicher
Begrenzung veröffentlicht werden (z.B.Ankündigung von Terminen, Abgabe von
Online-Dokumenten) nach Ablaufdatum automatisch von der Site entfernt und
archiviert werden. Ebenso können Inhalte oder Seiten, die ab einem bestimmten
Datum veröffentlicht werden sollen, zur Veröffentlichung hinterlegt werden.

Einbindung externer Datenquellen
Externe Datenquellen für Bilder oder andere Mediadateien können über Platzhalter
eingebunden werden.

Erweiterte Funktionalität
Die Einbindung von Modulen ermöglicht erweiterte Funktionalität, z.B.
Suchfunktionen über die Site.

Spezielle Anforderungen
Eine Besonderheit dieses CMS ist ein Listener, der bei Änderung von 
Jobfunktion. In regelmäßigen Zeitabständen
ablaufende Jobs sorgen dafür, dass Änderungen in der Datenbank, die eingebundene
Daten betreffen (z.B. Kontakt- oder Raumdaten), einen automatisierten Prozess in
Gang setzen, der die entsprechenden Seiten neu aus der Vorlage rendert und mit
einer neuen Versionsnummer versehen, widerum als statische Seiten abspeichert.
Je nach Volatilität der Daten kann die Frequenz der einzelnen Jobs angepasst
werden. Natürlich können diese Jobs auch per Hand angestossen werden, wenn zum
Beispiel ein Umbau oder ein Umzug multiple Änderungen notwendig gemacht hat.
Dieselbe Jobfunktion sorgt auch dafür, dass Beiträge mit zeitlicher Begrenzung,
regelmäßig in die Seiten eingepflegt, bzw. aus den Seiten gelöscht werden.

Darstellung der Organisation der Anwendung
Klassendiagramm zur Veranschaulichung der Kooperation zwischen den Klassen der
Anwendung und den Relationen der Datenbank.